<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8">

  <title><%= data.page.title || "Reveal.js Web Presentation" %></title>

  <meta name="description" content="<%= data.page.title || "Another web presentation" %>">
  <meta name="author" content="Daniel Jabbour">

  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />

  <%= stylesheet_link_tag "reveal/reveal" %>
  <%= stylesheet_link_tag "reveal/themes/#{data.page.theme || 'default'}", id: "theme" %>

  <!-- For syntax highlighting -->
  <%= stylesheet_link_tag "reveal/zenburn" %>

  <!-- If the query includes 'print-pdf', use the PDF print sheet -->
  <script>
    if ( window.location.search.match( /print-pdf/gi ) ) {
      document.write('<%= stylesheet_link_tag "reveal/print/pdf", media: "print" %>');
    } else {
      document.write('<%= stylesheet_link_tag "reveal/print/paper", media: "print" %>')
    }
  </script>

  <!--[if lt IE 9]>
    <%= javascript_include_tag "reveal/lib/html5shiv" %>
  <![endif]-->
</head>

<body>

<div class="reveal">

  <!-- Any section element inside of this container is displayed as a slide -->
  <div class="slides">

  <%= yield %>

  </div>

</div>

<%= javascript_include_tag "reveal/lib/head.min" %>
<%= javascript_include_tag "reveal/reveal" %>
<%= javascript_include_tag "hacktivism" %>

</body>
</html>
